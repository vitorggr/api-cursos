# Exemplo de configuração de variáveis de ambiente para a API Cursos
# Renomeie este arquivo para .env e ajuste os valores conforme seu ambiente

# Porta do servidor
PORT=3000

# Configuração do banco de dados MySQL (Clever Cloud ou local)
DB_HOST=seu_host_mysql
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=seu_banco
DB_PORT=3306

# Chave secreta para JWT
JWT_SECRET=sua_chave_secreta_segura
